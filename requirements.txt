# Verified end-to-end on 2026-06-13 (full report pipeline against real
# Gemini + Serper APIs). The AI agents now use direct REST calls — no
# crewai / crewai-tools / litellm — which removed a recurring class of
# dependency-resolution failures on deploy.
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-dotenv==1.2.2
supabase==2.30.0
requests==2.34.2
chess==1.11.2
stockfish==5.2.0
numpy==2.2.6
scikit-learn==1.7.0
apscheduler==3.11.2
